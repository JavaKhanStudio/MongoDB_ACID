services:
  mongo1:
    image: mongo:7.0
    container_name: mongo1
    command: --replSet rs0 --bind_ip_all
  mongo2:
    image: mongo:7.0
    container_name: mongo2
    command: --replSet rs0 --bind_ip_all
  mongo3:
    image: mongo:7.0
    container_name: mongo3
    command: --replSet rs0 --bind_ip_all
